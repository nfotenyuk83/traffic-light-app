{
  "scripts": {
    "dev": "next dev",
    "build": "gulp build && next build",
    "export": "gulp build && next build && next export",
    "deploy": "npm run export && touch out/.nojekyll && gh-pages -d out",
    "start": "next start",
    "lint": "next lint",
    "type-check": "tsc --noEmit",
    "gulp": "gulp",
    "watch": "gulp watch"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/react": "^18.2.45",
    "@types/react-dom": "^18.2.18",
    "gulp": "^4.0.2",
    "gulp-sass": "^5.1.0",
    "gulp-autoprefixer": "^8.0.0",
    "gulp-clean-css": "^4.3.0",
    "sass": "^1.69.5",
    "typescript": "^5.3.3",
    "gh-pages": "^6.1.0"
  }
}